export interface Skill {
  id: number;
  name: string;
  description: string;
  icon: string;
  color: string;
}

export interface Project {
  id: number;
  title: string;
  description: string;
  image: string;
  tags: string[];
  codeUrl?: string;
  demoUrl?: string;
}

export interface ContactLink {
  id: number;
  name: string;
  value: string;
  url: string;
  icon: string;
  color: string;
}

export interface Project {
  id: number;
  title: string;
  description: string;
  image: string;
  tags: string[];
  codeUrl?: string;
  demoUrl?: string;
}

export interface ContactLink {
  id: number;
  name: string;
  value: string;
  url: string;
  icon: string;
  color: string;
}